export default function Score () {
    const Score = {date, score}         // Setting props - date and score
    return (
         // Displaying the date abd score
        <div> 
        <p>Date: { data }</p>;         
        <p>Score: { score }</p>;        
        </div>
    );
}
